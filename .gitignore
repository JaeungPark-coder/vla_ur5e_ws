__pycache__/
*.pyc

# ROS2 build artifacts (vla_bridge)
build/
install/
log/

# openpi checkpoints / LeRobot datasets (large binaries, not committed)
checkpoints/
*.safetensors

# Collected demonstration data (collect_rlds_episodes.py writes GBs of .npy
# here; the LeRobot side lives outside the repo under HF_LEROBOT_HOME)
openvla_integration/raw_episodes/
models/
rl_logs/
*_eval_results.csv

# Editor
.vscode/
